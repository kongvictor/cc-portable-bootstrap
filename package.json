{
  "name": "cc-portable-bootstrap",
  "version": "1.1.4",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check core/bootstrap.mjs && node --check core/statusline/runtime.mjs && node --check core/statusline/configure.mjs && node --check core/statusline/snapshot.mjs",
    "check:posix": "sh -n scripts/setup-posix.sh && sh -n templates/claudex.sh && sh -n bin/statusline"
  }
}
